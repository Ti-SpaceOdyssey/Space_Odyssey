using UnityEngine;

public class BG_infinito : MonoBehaviour
{
    
    [SerializeField]
    private float distancia = -10f;

    private void Update()
    {
        if (transform.position.x < -distancia)
        {

            
        }
    }
}
